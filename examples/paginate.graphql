{
  hills(first: 5, after: "MQ==") {
    edges {
      cursor
      node {
        name
        number
      }
    }
    nodes
    pageInfo {
      endCursor
      startCursor

      hasNextPage
      hasPreviousPage
    }
  }
}
