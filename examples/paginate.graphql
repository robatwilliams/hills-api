{
  hills(first: 5, after: "MQ==") {
    edges {
      cursor
      node {
        names {
          primary
        }
        number
      }
    }
    nodes
    pageInfo {
      endCursor
      startCursor

      hasNextPage
      hasPreviousPage
    }
  }
}
