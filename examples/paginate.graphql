{
  hills(first: 5, after: "MQ==") {
    edges {
      cursor
      node {
        names
        number
      }
    }
    nodes
    pageInfo {
      endCursor
      startCursor

      hasNextPage
      hasPreviousPage
    }
  }
}
