const { DOUBLE, LONG, STRING } = require('./types');

module.exports = {
  countries: [STRING],
  heightFeet: DOUBLE,
  heightMetres: DOUBLE,
  lists: [STRING],
  number: LONG,
  parentMarilynNumber: LONG,
};
